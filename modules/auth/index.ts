/**
 * modules/auth
 *
 * Authentication feature module.
 * Contains: login form, register form, student ID verification upload,
 * OTP verification, and auth helper hooks.
 *
 * Sub-structure:
 *   components/   — AuthForm, IDUpload, OTPInput, VerificationStatus
 *   hooks/        — useAuth, useVerification
 *   actions/      — Server Actions for login/register (Next.js App Router)
 *   schema/       — Zod validation schemas
 */
export {};
