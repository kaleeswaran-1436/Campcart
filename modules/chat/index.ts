/**
 * modules/chat
 *
 * Real-time chat feature module.
 * Contains: conversation list, message thread, message input,
 * WebSocket connection management.
 *
 * Sub-structure:
 *   components/   — ConversationList, MessageThread, MessageInput, ChatBubble
 *   hooks/        — useConversations, useMessages, useSocket
 *   socket/       — Socket.io client setup
 */
export {};
