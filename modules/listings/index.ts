/**
 * modules/listings
 *
 * Listings feature module.
 * Contains: product card, listing detail, create/edit listing form,
 * filter sidebar, search bar, price range slider, image uploader.
 *
 * Sub-structure:
 *   components/   — ListingCard, ListingGrid, ListingDetail, CreateListingForm
 *   hooks/        — useListings, useListing, useCreateListing
 *   actions/      — Server Actions for create/update/delete
 *   schema/       — Zod validation for listing form
 */
export {};
