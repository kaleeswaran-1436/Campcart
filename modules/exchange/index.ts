/**
 * modules/exchange
 *
 * QR-based exchange feature module.
 * Contains: QR code generator, QR scanner, exchange status tracker,
 * meetup location picker.
 *
 * Sub-structure:
 *   components/   — QRCodeDisplay, QRScanner, ExchangeCard, StatusTimeline
 *   hooks/        — useExchange, useQRScanner
 *   actions/      — confirmExchange, completeExchange Server Actions
 */
export {};
